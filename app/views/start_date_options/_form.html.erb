<%= form_with(model: start_date_option, local: true) do |form| %>
  <% if start_date_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(start_date_option.errors.count, "error") %> prohibited this start_date_option from being saved:</h2>

      <ul>
        <% start_date_option.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
