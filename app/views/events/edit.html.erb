<div class="ml-0 mt-3 text-center">
  <h2>Edit "<%=@event.name%>" Event</h2>
</div>
<div class="ml-0 mt-2 text-center">
  <p>Please enter the required information to edit the "<%=@event.name%>" event:</p>
</div>
<%= render partial: "form",
           locals: {
               event: @event,
               submit_btn_text: "Update"
           } %>