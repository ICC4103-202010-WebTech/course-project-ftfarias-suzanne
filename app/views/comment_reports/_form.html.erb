<%= form_with(model: comment_report, local: true) do |form| %>
  <% if comment_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_report.errors.count, "error") %> prohibited this comment_report from being saved:</h2>

      <ul>
        <% comment_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
