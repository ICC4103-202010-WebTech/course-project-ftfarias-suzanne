<%= form_with(model: organization_administrator, local: true) do |form| %>
  <% if organization_administrator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization_administrator.errors.count, "error") %> prohibited this organization_administrator from being saved:</h2>

      <ul>
        <% organization_administrator.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
